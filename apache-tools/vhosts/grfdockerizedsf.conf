<VirtualHost *:80>
    ServerName grfdockerizedsf.local

    DocumentRoot /var/www/html/grf-dockerized-sf/public
    <Directory /var/www/html/grf-dockerized-sf/public>
        AllowOverride All
        Order Allow,Deny
        Allow from All
        FallbackResource /index.php
    </Directory>

    # uncomment the following lines if you install assets as symlinks
    # or run into problems when compiling LESS/Sass/CoffeeScript assets
    # <Directory /var/www/project>
    #     Options FollowSymlinks
    # </Directory>

    ErrorLog /var/log/apache2/grfdockerizedsf_error.log
    CustomLog /var/log/apache2/grfdockerizedsf_access.log combined
</VirtualHost>
